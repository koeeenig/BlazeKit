@code {

    public string MarkdownContent { get; set; }

    public Page()
    {
        MarkdownContent =
            new Yaapii.Atoms.Text.TextOf(
                new Yaapii.Atoms.IO.ResourceOf("Assets/getting-started.md", this.GetType())
            ).AsString();
    }
}
<PageTitle>BlazeKit - Getting Started</PageTitle>
<Markdown Text="@MarkdownContent"></Markdown>
