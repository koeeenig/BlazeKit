@using BlazeKit.Web.Components
@inject BlazeKit.Hydration.DataHydrationContext HydrationContext
@code  {
    [Inject]
    public IHostEnvironment HostEnvironment { get; set; }
}
<WebAssemblyLoadProgress class="linear-progress" />
@* @if(HostEnvironment.IsDevelopment())
{
    <BlazeKit.Website.Islands.Components.DevTools />
} *@
<BKitData />
@* <Microsoft.AspNetCore.Components.Sections.SectionOutlet SectionName="blazekit" /> *@
<Router AppAssembly="@typeof(Index).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(Routes.Layout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>BlazeKit - Not found</PageTitle>
        <LayoutView Layout="@typeof(Routes.Layout)">
            <h1>🤷 <small>Sorry, there's nothing at this address.</small></h1>
        </LayoutView>
    </NotFound>
</Router>
